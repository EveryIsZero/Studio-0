# ============================================================
# GitHub Linguist 语言统计配置
# ------------------------------------------------------------
# 本仓库是 Studio 0（0号工坊）的发行仓库：只存放说明文档、截图
# 与捐赠码；程序本体（Python / PyQt5）以 Release 资产形式分发，
# 源码不在仓库内。
#
# Linguist 按字节数统计语言。说明书 HTML 若参与统计，语言条会
# 显示为 "HTML 100%"，与实际项目语言不符，故标注为文档排除。
# ============================================================

# 说明书 HTML 属文档，不计入语言统计
*.html linguist-documentation

# README 系列（Linguist 默认已排除，此处显式声明以防规则变更）
README*.md linguist-documentation
